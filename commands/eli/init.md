---
name: "Eli: Init"
description: Initialize eli-spec directory structure and project config
category: Workflow
tags: [workflow, init, setup]
---

Initialize the `eli-spec/` directory in the current project. Auto-detects project context and generates `config.yaml`.

Run this once per project before using other `/eli:*` commands.

**Usage**: `/eli:init`

→ Delegates to skill `eli-init` for full implementation.
